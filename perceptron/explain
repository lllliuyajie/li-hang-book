numpy : numpy 的shape，reshape，zeros， dot(矩阵乘法)。  列表结构

切片: [:,:]  所有的行列向量，[0:,1:] 从第一行开始读取，从第二列开始读取， [:,0] 只读取第一列
time : time.time()当前时间戳  ，time.scrfptime()接收元组，表示字符串（format）

random ： random.uniform(a, b)用于生成指定范围内的浮点数(a>b)    random.randint(a, b)用于生成指定范围内的整型

pandas：（字典结构） 导入csv或者xlsx文件  pandas.read("文件路径"，header = )  读出的数据是字典形式